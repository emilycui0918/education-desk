# Education Desk

Private-by-device education workspace hosted as a public static shell.

The repository contains no personal workspace data. A valid private JSON backup must be imported in the browser before any content is shown. Data is stored only in that browser's local storage and can be exported again from the site.

GitHub Pages URL: `https://emilycui0918.github.io/education-desk/`
